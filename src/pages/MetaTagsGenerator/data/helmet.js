import { buildPageUrl, buildOgImageUrl } from '../../../config/site'

export const SLUG       = 'meta-tags-generator'
export const PAGE_URL   = buildPageUrl(SLUG)
export const OG_IMAGE   = buildOgImageUrl(SLUG)
export const PAGE_TITLE = 'Free SEO Meta Tag Generator Online — Title, Description, OG Tags | Abect'
export const PAGE_DESC  = 'Generate SEO meta tags instantly — title, description, canonical, robots, hreflang, Open Graph. Live Google preview and copy-ready HTML. Free, no signup.'
