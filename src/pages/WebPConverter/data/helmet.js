import { buildPageUrl, buildOgImageUrl } from '../../../config/site'

export const SLUG       = 'webp-converter'
export const PAGE_URL   = buildPageUrl(SLUG)
export const OG_IMAGE   = buildOgImageUrl(SLUG)
export const PAGE_TITLE = 'Free WebP Converter Online: JPG, PNG, GIF, AVIF to WebP | Abect'
export const PAGE_DESC  = 'Convert JPG, PNG, GIF, AVIF and BMP to WebP free — 25–34% smaller files, instant, in your browser. No uploads, no signup. Try it now.'
