import { buildPageUrl, buildOgImageUrl } from '../../../config/site'

export const SLUG       = 'favicon-generator'
export const PAGE_URL   = buildPageUrl(SLUG)
export const OG_IMAGE   = buildOgImageUrl(SLUG)
export const PAGE_TITLE = 'Free Favicon Generator Online — From Text, Emoji or Image | Abect'
export const PAGE_DESC  = 'Generate favicon files for any website — from text, emoji, or logo. Get all sizes: 16×16, 32×32, 180×180, 512×512. Free, instant, no uploads.'
