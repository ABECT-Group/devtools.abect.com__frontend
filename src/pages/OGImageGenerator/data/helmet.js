import { buildPageUrl, buildOgImageUrl } from '../../../config/site'

export const SLUG       = 'og-image-generator'
export const PAGE_URL   = buildPageUrl(SLUG)
export const OG_IMAGE   = buildOgImageUrl(SLUG)
export const PAGE_TITLE = 'Free OG Image Generator — Crop to 1200×630 & Get Meta Tags | Abect'
export const PAGE_DESC  = 'Upload any image, crop it to 1200×630 px for Open Graph, and get copy-ready og:image meta tags instantly. Free, private, runs in your browser — no signup.'
